{
  "name": "digigo",
  "version": "0.0.1",
  "license": "MIT",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/**/*"
    ]
  },
  "scripts": {
    "dev": "run-p dev:*",
    "dev:app": "yarn workspace @digigo/app dev",
    "prebuild": "cd account-abstraction/packages/contracts && yarn build && cd ../../..",
    "build": "run-p build:*",
    "build:app": "yarn workspace @digigo/app build",
    "lint": "run-p lint:*",
    "lint:app": "yarn workspace @digigo/app lint"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
